
{% block body %}
<main class="col-md-9 ms-sm-auto col-lg-10 px-md-4">
{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
</main>
{% endblock %}
